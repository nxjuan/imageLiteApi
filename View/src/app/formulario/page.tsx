import { Template } from '@/components'

export default function Formulario(){
    return (
        <Template>
            <h1 className="text-gray-600">Formularios</h1>
        </Template>
    )
    
}