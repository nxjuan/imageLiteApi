'use client'
import { Template } from "@/components";

export default function NotFoundPage(){
    return(
        <Template>
            <section className="text-gray-900 text-x1 text-center">
                <h1>Page not found!</h1>
            </section>
        </Template>
    )
}